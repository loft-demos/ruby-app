apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vcluster-env-config
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters:
        selector:
          matchLabels:
            env: dev
    - clusters:
        selector:
          matchLabels:
            env: qa
    - clusters:
        selector:
          matchLabels:
            env: prod
  template:
    metadata:
      name: ruby-app-{{.name}}
    spec:
      destination:
        namespace: ruby-app-{{.metadata.labels.env}}
        name: '{{.name}}'
      info:
        - name: Git Repo
          value: 'https://github.com/loft-demos/ruby-app/'
      project: 'loft-{{ index .metadata.labels "demos.vcluster.com/project" }}'
      source:
        path: vcluster-use-cases/argocd-vcluster-add-ons/manifests/{{.metadata.labels.env}}/
        repoURL: 'https://github.com/loft-demos/ruby-app.git'
        targetRevision: 'main'
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
