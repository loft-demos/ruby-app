apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: "ruby-app-pr"
  namespace: argocd
spec:
  generators:
    - pullRequest:
        gitea:
          owner: "loft-demos"
          repo: "ruby-app"
          api: "https://github.com"
          insecure: true
          tokenRef:
            secretName: loft-demo-org-cred
            key: token
        requeueAfterSeconds: 30
  template:
    metadata:
      name: "ruby-app-{{branch}}-pr{{number}}"
    spec:
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
      source:
        repoURL: "https://github.com/loft-demos/ruby-app.git"
        targetRevision: "{{head_sha}}"
        path: helm-chart/
        helm:
          parameters:
            - name: "image.repository"
              value: "ghcr.io/loft-demos/ruby-app-demo-app"
            - name: "image.tag"
              value: "{{head_short_sha}}"
            - name: "image.args.text"
              value: "Hello from ruby-app pr-{{number}} commit {{head_short_sha}}"
            - name: "ingress.hosts[0].host"
              value: "ruby-app-pr-{{number}}-ruby.us.demo.dev"
            - name: ingress.hosts[0].paths[0].backend.service.name
              value: juliet-app
            - name: ingress.hosts[0].paths[0].backend.service.port.name
              value: http
            - name: ingress.hosts[0].paths[0].path
              value: /
            - name: ingress.hosts[0].paths[0].pathType
              value: prefix
            - name: "ingress.tls[0].hosts[0]"
              value: "ruby-app-pr-{{number}}-ruby.us.demo.dev"
      project: "default"
      destination:
        server: "https://ruby.us.demo.dev/kubernetes/project/default/virtualcluster/ruby-app-pr-{{number}}"
        namespace: preview-hello-world-app
      info:
        - name: Preview App Link
          value: >-
            https://ruby-app-pr-{{number}}-ruby.us.demo.dev
        - name: Pull Request
          value: >-
            https://github.com/loft-demos/ruby-app/pulls/{{number}}
